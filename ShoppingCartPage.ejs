<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart Page</title>
    <link rel="stylesheet" href="css/cart.css">
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.3/dist/sweetalert2.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11.7.3/dist/sweetalert2.all.min.js"></script>
</head>

<body>
    <header>
        <button onclick="toggleMenu()" class="menu">&#9776;</button>
        <h1><a href="/">好吃烘培工坊</a></h1>
        <nav>
            <ul id="menuItems">
                <li><a href="/ShoppingCartPage">購物車</a></li>
                <li><a href="/LoginPage">後台管理</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <section class="product">
            <div class="header-div"></div>
            <% if (products.length > 0) { %>
                <% for (var i=0; i < products.length; i++) { %>
                    <div class="product-content">
                        <div class="cart-cancel">
                            <img src="images/cancel.png" alt="">
                        </div>
                        <div class="cart-image">
                            <img src="uploads/<%= products[i].image %>">
                        </div>
                        <div class="cart-name"><%= products[i].name %></div>
                        <div class="cart-price">NT$ <%= products[i].price %></div>
                        <div>$<span class="item-price">0</span></div>
                        <div class="cart-quantity">
                            <button class="minus-btn">-</button>
                            <span class="quantity">0</span>
                            <button class="plus-btn">+</button>
                        </div>
                    </div>
                <% } %>
                <div class="total-price-box">
                    <div>Total Price :</div>
                    <div>$ <span id="total-price">0</span></div>
                    <div onclick="toFinalCheckout()" class="checkout">去買單</div>
                </div>
            <% } else { %>
                <img src="images/empty_cart.png" alt="">
            <% } %>
        </section>
    </main>

    <script src="js/cart.js"></script>
    
</body>

</html>